function fight () {
    // User input variables;
    const monster = prompt("What's the name of the monster's name?: ");
    const hero = prompt("What's the hero's name: ");
    // Character health variables;
    let villainHealth = 50;
    let heroHealth = 50;
    let heroDiceRoll = Math.random()

    
};